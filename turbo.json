{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**"],
      "cache": false
    }
  }
}
